2016.4.17
这个是web前端的相似度模型参数提交和文件读取，具体的文件的存放还需要再具体一些

2016.4.28
对于具体数据读入的代码实现，轨迹和轨迹点，对于不同类型的文件读取已修改完毕，对于出错问题已修改完毕


2016.4.28
其他的实现不用管，在action里面DoubleTrajAction.java定义了两个轨迹的接收和轨迹相似度结果的计算，现在，在此类里面可获取了其点的具体的信息。现在问题是在DTWSimilarity.java里面定义DTW的相似度计算的接口，在action里面调用即可返回相似度。


2016.5.3
相似度已集成成功，但是还有些具体的小问题。csv文件读取有格式问题